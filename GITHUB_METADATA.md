# GitHub metadata

Use the following values when creating or polishing the GitHub repository.

## Repository name

`zotero-vectorize`

## Short description (GitHub "About")

**Option A (recommended)**

Cross-platform OpenClaw skill for building and maintaining a local Zotero semantic index with metadata embeddings, PDF full-text chunk embeddings, and safe incremental updates.

**Option B (shorter)**

Cross-platform OpenClaw skill for Zotero metadata/full-text vectorization, incremental updates, and local semantic search workflows.

## Website

Leave empty for now unless you later publish docs or a demo page.

## Topics

Recommended topics:

- `openclaw`
- `skill`
- `zotero`
- `embeddings`
- `vector-search`
- `semantic-search`
- `rag`
- `pdf`
- `literature-review`
- `research`
- `knowledge-management`
- `cross-platform`
- `python`

## Suggested repository subtitle / first sentence

A local-first OpenClaw skill that turns a Zotero library into reusable metadata and full-text vector stores for semantic retrieval and RAG.

## Suggested README opening blurb

`zotero-vectorize` is a cross-platform, local-first OpenClaw skill for turning a Zotero library into two reusable vector stores: one over bibliographic metadata and one over chunked PDF full text. It also includes a safe incremental-update workflow with diff reporting, backup retention, and verification helpers.

## Suggested social preview text

Turn your Zotero library into metadata + full-text vector stores for semantic search and RAG — with safe incremental updates and OpenClaw skill packaging built in.

## Suggested pinned features (for the repo page or release body)

- Cross-platform: Windows, macOS, Linux
- Local-first Zotero workflow
- Metadata embeddings + PDF full-text chunk embeddings
- Incremental diff → confirm → backup → append workflow
- Backup retention: latest + previous only
- Skill validation and packaging included
